clone this repo using: `git clone https://github.com/ramanand-rv/samvad-sql.git`

move to project folder: `cd samvad-sql`

install required packages: `pip install -r requirements.txt`

run backend: `uvicorn src.api.main:app --reload`

run client: `streamlit run .\src\ui\streamlit_app.py `
